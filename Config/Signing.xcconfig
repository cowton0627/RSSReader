// Signing.xcconfig — tracked by git.
//
// Keeps personal Team IDs out of project.pbxproj, which is shared. The real
// value lives in Signing.local.xcconfig, which git ignores.
//
// First time on a new machine:
//   cp Config/Signing.local.xcconfig.example Config/Signing.local.xcconfig
// then fill in your Team ID (Xcode > Settings > Accounts). Simulator builds
// do not need it — only signing for a real device does.

#include? "Signing.local.xcconfig"

CODE_SIGN_STYLE = Automatic
DEVELOPMENT_TEAM = $(LOCAL_DEVELOPMENT_TEAM)
